#===============================================
#MongoDB Root Credentials (for initialization)
#===============================================
#Используются скриптом docker-entrypoint для создания root-пользователя
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example

#===============================================
#Application Database Credentials
#===============================================
#Эти переменные должны совпадать с теми, что вы используете в init-mongo.js
#для создания пользователя приложения.
MONGO_APP_USER=appuser
MONGO_APP_PASS=apppass
MONGO_DB_NAME=mydb

#===============================================
#Application & Connection Settings
#===============================================
#Хост БД внутри Docker-сети
MONGO_HOST=mongo

#Стандартный порт MongoDB
MONGO_PORT=27017

#Порт, на котором будет доступно ваше Spring Boot приложение
APP_PORT=8080